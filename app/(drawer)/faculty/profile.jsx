import { View, Text } from "react-native";

export default function FacultyProfile() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>My Profile (Faculty Only)</Text>
    </View>
  );
}
